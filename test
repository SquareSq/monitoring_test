#!/bin/bash

# Бесконечный цикл, который считает от 1 до бесконечности
count=1
while true; do
    echo $count
    count=$((count + 1))  # Увеличение счетчика на 1
    sleep 3  # Задержка в 3 секунды для удобства чтения
done